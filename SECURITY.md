# Security Policy

Do not open a public issue containing passwords, payment keys, callback URLs, tokens, database exports, or user data.

For a credential exposure in this practice project, remove the credential from the local configuration, rotate it at the provider, and contact the repository owner privately. Removing a secret from a later commit does not make it safe if it was already pushed.
